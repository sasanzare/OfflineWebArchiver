# Project Format Knowledge

Verified authority is `docs/architecture/PROJECT_FORMAT.md` plus the strict runtime schema in `packages/project-format`. Format is 1.0.0; all persisted internal paths are portable relative paths. Reserved future profile/auth/proxy roots are not Phase 4 capability claims.
