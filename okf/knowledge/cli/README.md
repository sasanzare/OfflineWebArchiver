# CLI Knowledge

The production CLI supports Project, Profile, Scope, and all 14 Queue commands in human or JSON form. Queue list/history pagination is bounded; duplicate/rejected results are structured; validation and business errors use stable exits. Explicit Project selection, redaction, and contract 1.3.0 preserve the Application Service boundary. Claims/completions are controlled simulations, not a crawler.
