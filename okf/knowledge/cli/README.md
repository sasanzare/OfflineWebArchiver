# CLI Knowledge

The production CLI supports help, version, and `system describe` in human or JSON form. Stable exit codes and the shared contract make it suitable for diagnostics without bypassing the application service.
