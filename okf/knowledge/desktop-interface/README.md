# Desktop Interface Knowledge

The production Electron shell is English-only and exposes one narrow `systemDescribe` preload method. Renderer isolation, sandboxing, sender validation, navigation denial, permission denial, and no remote content are directly tested.
